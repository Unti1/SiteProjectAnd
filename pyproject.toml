[tool.poetry]
name = "siteproject"
version = "0.1.0"
description = ""
authors = ["Artyom"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
django = "^4.2.5"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
